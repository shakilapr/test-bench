# Copy to .env and fill in. Do not commit .env.
# Backend
NODE_ENV=development
PORT=3000
SQLITE_PATH=./data/bench.sqlite

# MQTT broker. Embedded by default; set EMBED_BROKER=false to use an external one.
EMBED_BROKER=true
EMBED_BROKER_PORT=1883
MQTT_URL=mqtt://localhost:1883
MQTT_USER=
MQTT_PASS=
